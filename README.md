This project is a JSON Editor built with Next.js that allows you to convert JSON schemas into dynamic form UIs.
You can easily fill in values through the generated form and then retrieve the output JSON with properties and their corresponding values.

🚀 Features

Convert any JSON schema into a responsive form UI

Edit and update form data dynamically

Get real-time JSON output with property-value pairs

Built using Next.js and @json-editor/json-editor